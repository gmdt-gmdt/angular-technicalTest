import { StrikeTextPipe } from './strike-text.pipe';

describe('StrikeTextPipe', () => {
  it('create an instance', () => {
    const pipe = new StrikeTextPipe();
    expect(pipe).toBeTruthy();
  });
});
